{% extends 'gui/base.html.twig' %}

{% block title %}Onduleurs | Inventaire{% endblock %}

{% block body %}
    <div class="d-flex mb-4 mt-3">
        <span class="fa-stack me-2 ms-n1">
            <i class="fas fa-circle fa-stack text-300"></i>
            <i class="fa-inverse fa-stack-1x text-danger fas fa-car-battery"></i>
        </span>
        <div class="col">
            <h5 class="mb-0 text-danger position-relative">
                <span class="bg-200 dark__bg-1100 pe-3">Onduleurs</span>
                <span class="border position-absolute top-50 translate-middle-y w-100 start-0 z-n1"></span>
            </h5>
            <p class="mb-0">Cette page vous permet de voir tous les onduleurs.</p>
        </div>
    </div>
    <div class="row g-0">
        <div class="col-lg-12 ps-lg-2">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <strong>Parfait :</strong> {{ message }}
                    <button class="btn-close" type="button" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            {% endfor %}
            <div class="sticky-sidebar">
                <div class="card mb-3">
                    <div class="card-header d-flex flex-between-center">
                        <h5 class="mb-0">Toutes les onduleurs</h5>
                        <a class="btn btn-falcon-default btn-sm" href="{{ path('admin.onduleur.add') }}">
                            Ajouter
                        </a>
                    </div>
                    <div class="card-body bg-light">
                        <div id="table_onduleur" data-list='{"valueNames":["nom","marque","modele","numero_serie","capacite","type_prise","emplacement","site","fournisseur","etat","date_installation","date_achat","date_garantie","commentaire"],"page":20,"pagination":true}'>
                            <div class="row justify-content-end g-0">
                                <div class="col-auto col-sm-5 mb-3">
                                    <div class="input-group">
                                        <input class="form-control form-control-sm shadow-none search" type="search" placeholder="Rechercher..." aria-label="search" pattern="-*" />
                                        <div class="input-group-text bg-transparent">
                                            <span class="fa fa-search fs--1 text-600"></span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="table-responsive scrollbar">
                                <table class="table overflow-hidden mb-2">
                                    <thead class="bg-200 text-900">
                                        <tr>
                                            <th class="align-middle text-center">#</th>
                                            <th class="align-middle text-center">Nom</th>
                                            <th class="align-middle text-center">Marque</th>
                                            <th class="align-middle text-center">Modèle</th>
                                            <th class="align-middle text-center">Numéro de série</th>
                                            <th class="align-middle text-center">Capacité</th>
                                            <th class="align-middle text-center">Type de prise</th>
                                            <th class="align-middle text-center">Emplacement</th>
                                            <th class="align-middle text-center">Site</th>
                                            <th class="align-middle text-center">Fournisseur</th>
                                            <th class="align-middle text-center">État</th>
                                            <th class="align-middle text-center">Date d'installation</th>
                                            <th class="align-middle text-center">Date d'achat</th>
                                            <th class="align-middle text-center">Date de garantie</th>
                                            <th class="align-middle text-center">Commentaire</th>
                                            <th class="align-middle text-center">Action</th>
                                        </tr>
                                    </thead>
                                    <tbody class="list">
                                        {% if onduleurs | length != 0 %}
                                            {% for onduleur in onduleurs %}
                                                <tr class="align-middle text-center">
                                                    <td class="text-nowrap">{{ loop.revindex }}</td>
                                                    <td class="text-nowrap nom">
                                                        {{ onduleur.nom }}
                                                    </td>
                                                    <td class="text-nowrap marque">
                                                        {% if onduleur.marque != null %}
                                                            {{ onduleur.marque }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap modele">
                                                        {% if onduleur.modele != null %}
                                                            {{ onduleur.modele }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap numero_serie">
                                                        {% if onduleur.numero_serie != null %}
                                                            {{ onduleur.numero_serie }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap capacite">
                                                        {% if onduleur.capacite != null %}
                                                            {{ onduleur.capacite }}W
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap type_prise">
                                                        {% if onduleur.type_prise != null %}
                                                            {{ onduleur.type_prise }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap emplacement">
                                                        {% if onduleur.emplacement_nom != null %}
                                                            {{ onduleur.emplacement_nom }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap site">
                                                        {% if onduleur.entreprise_nom != null %}
                                                            {{ onduleur.entreprise_nom }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap fournisseur">
                                                        {% if onduleur.fournisseur_nom != null %}
                                                            {{ onduleur.fournisseur_nom }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap etat">
                                                        {% if onduleur.etat_nom != null %}
                                                            {{ onduleur.etat_nom }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap date_installation">
                                                        {% if onduleur.date_installation != null %}
                                                            {{ onduleur.date_installation }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap date_achat">
                                                        {% if onduleur.date_achat != null %}
                                                            {{ onduleur.date_achat }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap date_garantie">
                                                        {% if onduleur.date_garantie != null %}
                                                            {{ onduleur.date_garantie }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap commentaire">
                                                        {% if onduleur.commentaire != null %}
                                                            {{ onduleur.commentaire }}
                                                        {% else %}
                                                            <span class="badge text-bg-danger">N/A</span>
                                                        {% endif %}
                                                    </td>
                                                    <td class="text-nowrap">
                                                        <a href="{{ path('admin.onduleur.edit', {id: onduleur.id}) }}" class="btn btn-primary btn-sm">
                                                            <span class="bi bi-pencil-square"></span>
                                                        </a>

                                                        <form method="post" action="{{ path('admin.onduleur.delete', {id: onduleur.id}) }}" style="display: inline-block" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer l\'onduleur : {{ onduleur.nom }} ?')">
                                                            <input type="hidden" name="_method" value="DELETE">
                                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ onduleur.id) }}">
                                                            <button class="btn btn-danger btn-sm">
                                                                <span class="bi bi-trash-fill"></span>
                                                            </button>
                                                        </form>
                                                    </td>
                                                </tr>
                                            {% endfor %}
                                        {% else %}
                                            <tr class="align-middle text-center">
                                                <td colspan="12">Aucun onduleur n'a été trouvé.</td>
                                            </tr>
                                        {% endif %}
                                    </tbody>
                                </table>
                            </div>
                            <div class="d-flex justify-content-center mt-3">
                                <button class="btn btn-sm btn-falcon-default me-1" type="button" title="Précédent" data-list-pagination="prev">
                                    <span class="fas fa-chevron-left"></span>
                                </button>
                                <ul class="pagination mb-0"></ul>
                                <button class="btn btn-sm btn-falcon-default ms-1" type="button" title="Suivant" data-list-pagination="next">
                                    <span class="fas fa-chevron-right"></span>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}